# Colleen's AeroSpace config, based closely on the setup shared by her friend.
config-version = 2

after-startup-command = [
    'exec-and-forget sketchybar',
    'exec-and-forget borders hidpi=on active_color=0xff7fbfff inactive_color=0xff494d64 width=5.0',
]

exec-on-workspace-change = [
    '/bin/bash',
    '-c',
    '/opt/homebrew/opt/sketchybar/bin/sketchybar --trigger aerospace_workspace_change AEROSPACE_FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE AEROSPACE_PREV_WORKSPACE=$AEROSPACE_PREV_WORKSPACE',
]

start-at-login = true

enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true
accordion-padding = 30
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'

on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = ['move-mouse window-lazy-center']
automatically-unhide-macos-hidden-apps = true

persistent-workspaces = [
    '1', '2', '3', '4', '5', '6', '7', '8', '9',
    'I', 'M', 'O', 'P', 'U',
]

on-mode-changed = [
    'exec-and-forget /opt/homebrew/opt/sketchybar/bin/sketchybar --trigger aerospace_mode_change',
]

[key-mapping]
preset = 'qwerty'

[gaps]
inner.horizontal = 0
inner.vertical = 0
outer.left = 0
outer.bottom = 0
outer.top = [{ monitor."studio" = 30 }, { monitor."built-in" = 0 }, 0]
outer.right = 0

[mode.main.binding]

# Layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
alt-f = 'layout floating tiling'

# Focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# Move windows
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Join containers
alt-ctrl-h = ['join-with left', 'mode main']
alt-ctrl-j = ['join-with down', 'mode main']
alt-ctrl-k = ['join-with up', 'mode main']
alt-ctrl-l = ['join-with right', 'mode main']

# Resize
alt-minus = 'resize smart -50'
alt-equal = 'resize smart +50'

# Numbered workspaces
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-4 = 'workspace 4'
alt-5 = 'workspace 5'
alt-6 = 'workspace 6'
alt-7 = 'workspace 7'
alt-8 = 'workspace 8'
alt-9 = 'workspace 9'

# Letter workspaces from the friend's setup
alt-i = 'workspace I'
alt-m = 'workspace M'
alt-o = 'workspace O'
alt-p = 'workspace P'
alt-u = 'workspace U'

# Move windows to numbered workspaces
alt-shift-1 = 'move-node-to-workspace 1'
alt-shift-2 = 'move-node-to-workspace 2'
alt-shift-3 = 'move-node-to-workspace 3'
alt-shift-4 = 'move-node-to-workspace 4'
alt-shift-5 = 'move-node-to-workspace 5'
alt-shift-6 = 'move-node-to-workspace 6'
alt-shift-7 = 'move-node-to-workspace 7'
alt-shift-8 = 'move-node-to-workspace 8'
alt-shift-9 = 'move-node-to-workspace 9'

# Move windows to letter workspaces
alt-shift-i = 'move-node-to-workspace I'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-o = 'move-node-to-workspace O'
alt-shift-p = 'move-node-to-workspace P'
alt-shift-u = 'move-node-to-workspace U'

alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'
alt-shift-semicolon = 'mode service'

[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main']
f = ['layout floating tiling', 'mode main']
backspace = ['close-all-windows-but-current', 'mode main']
alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

# Primary workspaces
[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
if.during-aerospace-startup = true
run = ['move-node-to-workspace I', 'move left']

[[on-window-detected]]
if.app-name-regex-substring = 'Cursor'
if.during-aerospace-startup = true
run = ['move-node-to-workspace I', 'move right']

[[on-window-detected]]
if.app-id = 'com.google.Chrome'
if.during-aerospace-startup = true
run = ['move-node-to-workspace P']

[[on-window-detected]]
if.app-name-regex-substring = 'Notion'
run = ['move-node-to-workspace O']

# Supporting apps
[[on-window-detected]]
if.app-name-regex-substring = 'Linear'
run = ['move-node-to-workspace 1']

[[on-window-detected]]
if.app-name-regex-substring = 'Zoom'
run = ['move-node-to-workspace 4']

[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = ['move-node-to-workspace 7']

[[on-window-detected]]
if.app-name-regex-substring = 'Claude'
run = ['move-node-to-workspace 8']

[[on-window-detected]]
if.app-id = 'com.spotify.client'
run = ['move-node-to-workspace 9']

[[on-window-detected]]
if.app-id = 'com.apple.MobileSMS'
run = ['layout floating']

# Refresh SketchyBar whenever a window appears
[[on-window-detected]]
if = 'true'
run = [
    'exec-and-forget /opt/homebrew/opt/sketchybar/bin/sketchybar --trigger aerospace_workspace_change',
]

# Keep the friend's multi-monitor behavior.
[workspace-to-monitor-force-assignment]
4 = ['secondary', 'built-in']
7 = ['secondary', 'built-in']
8 = ['secondary', 'built-in']
9 = ['secondary', 'built-in']
U = ['secondary', 'built-in']
